(function() {
  'use strict';

  angular
    /**
     * @namespace toast
     * @memberof source
     *
     * @description
     * Definition of module "toast" for alert messages services.
     */
    .module('source.toast', [
      /* External Modules */
      'toastr'
    ]);
})();
